spring:
# spring boot 에서 없으면 다 memory 모드로 돌
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#  create -> 드랍 한 후 create 하고 어플 시행
#  create-drop -> 같은데 마지막에 종료시점에 drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace